{
    "request": {
    "urlPattern": "/petstore.swagger.io/v2/pet/[0-9]{1}/uploadImage",
    "method": "POST"
    },

   "response": {
    "status": 200,
    "jsonBody": {
      "code": 200,
      "type": "unknown",
      "message": "additionalMetadata: null\nFile uploaded to ./filename.type, filesize bytes"
    },
    "headers": {
      "Content-Type": "application/json",
      "Date": "{{now format = 'EEE, dd MMM yyyy HH:mm:ss z'}}"
    }
  }
}